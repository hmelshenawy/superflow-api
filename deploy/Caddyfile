# Deprecated for this VPS.
# This host already uses Traefik on ports 80/443, so SuperFlow now integrates with Traefik instead.
# Keep this file only as a reference example if you ever move the project to a non-Traefik host.
